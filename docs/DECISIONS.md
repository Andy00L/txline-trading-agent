# Decision log

Format: `YYYY-MM-DD | decision | why | alternative rejected`. Newest at top. Never delete an entry; supersede it with a new line.

2026-06-27 | Primary signal is now a cross-market goals-model relative-value bet, superseding steam-following as the default | a single de-margined consensus cannot be out-forecast, and steam-following measured CLV-negative (refuted across 180k+ matches in the research pass); the free World Cup tier serves a full surface per fixture (1X2 + an Over/Under total-goals ladder + an Asian-Handicap ladder, confirmed by a live market-taxonomy probe 2026-06-27), so fitting one Dixon-Coles supremacy/total scoreline model jointly to the surface and backing the 1X2 leg the joint fit prices longer than the line (Kaunitz et al. 2017) is a defensible relative-value signal sized by a real positive Kelly edge | steam-following on the consensus line (CLV-negative), single-market de-vig divergence (1X2 alone is exactly identified, zero residual, no signal)
2026-06-27 | Cross-market entry is gated to the near-kickoff window (kickoff read from the scores channel, 5 min to 6 h before start) and Closing Line Value is measured against the last PRE-kickoff consensus | a thin far-from-kickoff market produces false signals, and a real closing line only forms near kickoff; an in-play price (the draw probability collapses after a goal) is not a closing line | trading any time a surface exists (thin-market noise), measuring CLV against the latest observation (in-play contamination)
2026-06-27 | Settlement finality is derived from the numeric StatusId, not the textual GameState | the /updates (replay) feed freezes GameState at "scheduled" through the whole match; only the numeric StatusId transitions to the final-whistle code (5 F / 10 FET / 13 FPE), confirmed from a fixture-status probe (2116/2116 records carried it) | reading GameState, which never reaches a final string on the replay feed, so nothing settled
2026-06-25 | Primary signal is steam plus Closing Line Value on the StablePrice consensus line; cross-book divergence is an enhancement gated on seeing more than one BookmakerId per market | the free World Cup tier serves de-margined consensus odds (effectively probabilities) per the tx-on-chain README and odds overview, so single-book cross-sectional divergence is likely unavailable, while the time-series steam and CLV signal is fully defensible on consensus-only data and de-risks the demo | depending on multi-book data the free tier may not carry (see M0-recon-findings.md). SUPERSEDED 2026-06-27 by the cross-market goals model.
2026-06-25 | Keep commit-reveal plus validate_stat CPI even though devnet IDL 1.5.2 exposes create_trade and settle_trade | those implement a two-party real-SOL-staked peer wager that needs a counterparty and real funds, whereas this agent is solo and paper-only and needs a non-cherry-picked record that commit-before-kickoff plus oracle-proven settle provides | using settle_trade, which needs a counterparty and real value
2026-06-25 | Strategy archetype: consensus-divergence plus sharp-move, primary market 1X2 full-time | TxLINE serves consensus odds, so divergence from devigged fair value is the defensible signal and the easiest edge to prove via Closing Line Value before the deadline | in-play market maker (harder to prove edge pre-deadline), agent-vs-agent (splits effort across two strategies)
2026-06-25 | Trust model: commit-reveal track record settled by CPI into txoracle validate_stat | validate_stat is CPI-able (one read-only account, reverts on a bad proof), so PnL is only writable when the oracle-attested score matches the committed claim; inputs, decisions, and outcomes are all auditable on-chain | own escrow or settlement (none exists in the oracle), off-chain-only PnL (not trustless, cherry-pickable)
2026-06-25 | Judge access: web dashboard plus a devnet program and endpoint | strongest demo given matches end before judging; a console shows the full ingest to settle flow with explorer links | API-only access (weaker visual demo)
2026-06-25 | Stack: TS monorepo (pnpm plus Turborepo), @solana/kit, Anchor 0.31.1 program agent_ledger, Vite plus React dashboard | current Solana tooling; one language across the trust chain; Anchor 0.31.1 matches the txoracle repo; reuse the copied design system | Next.js (server rendering not needed for a local-API console); a Rust off-chain core (TS keeps feed, quant, and client cohesive and testable)
2026-06-25 | One Feed interface with LiveSseFeed and ReplayFeed; identical strategy code path | a green walk-forward backtest is then direct evidence about live behavior, which is the core of the demo since there are no live matches at judging | separate live and backtest code paths (a backtest would not prove the live system)
2026-06-25 | Money as integer minor units (MicroUsd bigint), odds as DecimalOddsMilli (integer x1000) | floating point for money is a bug class; integers keep the accounting and the chain boundary exact | floats for stakes and bankroll
2026-06-25 | On-chain storage Mode A (one DecisionCommit PDA per decision) for the demo | best judge UX, a queryable account and event per decision, about 0.65 SOL on devnet for the full tournament | Mode B (rolling commit_log_root, constant storage) held in reserve if scale demands; no schema change to switch
